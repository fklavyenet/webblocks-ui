{
  "name": "webblocks-ui-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "lint": "node tests/lint.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
